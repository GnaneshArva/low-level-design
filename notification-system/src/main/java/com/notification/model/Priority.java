package com.notification.model;

/**
 * Priority levels for notification ordering.
 * Higher ordinal = higher priority in processing queues.
 */
public enum Priority {
    LOW, MEDIUM, HIGH, CRITICAL
}
